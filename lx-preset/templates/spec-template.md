# SPEC [NNN] — [SHORT_TITLE]

<!--
  LX SPEC. Заполняется командой /speckit.specify.
  SPEC отвечает на «ЧТО и ЗАЧЕМ» (включая инварианты — durable-контракт), а не «КАК» (это PLAN).
  Язык прозы — русский; идентификаторы кода/полей — как в коде (английский).
  Спецификация — живой документ: остаётся в репозитории навсегда как слой документации
  и точка входа в намерение. Раскладка секций выровнена по Kiro requirements.md
  (см. docs/KIRO_ALIGNMENT.md). Формат v4.2: секции НЕ нумеруются — иерархию несут
  уровни ##/###/####; точные ссылки — по ID сущностей (R1, INV2, D1) и именам секций
  (номера нестабильны: опциональные секции опускаются по-разному в каждой спеке).

  ДВА ЖАНРА (маркер `Жанр` во front-matter):
  • Продукт (P) — ценность идёт от пользователя/бизнеса; SPEC содержателен, PLAN лёгкий.
  • Инженерный (E) — «требование» само по себе техническое; SPEC может быть тонким,
    центр тяжести — «Требования» + «Инварианты». «Текущее состояние» для E — фактически обязательно.

  ВИЗУАЛЬНЫЕ КОНВЕНЦИИ (образец — Leadaxe SPEC; всё рендерится на GitHub). Действуют и на PLAN.
  Правило: каждый приём делает что-то СКАНИРУЕМЫМ за секунду; декор ради декора — убирай.
  • Проза — короткими абзацами, воздух между блоками; на каждый идентификатор/путь — инлайн `code`.
  • Схемы / layout / config / on-disk-дерево / pipeline — в fenced-блоках (```jsonc/go/text/json```), НЕ прозой.
  • Сравнения / семантика / перечни — таблицами.
  • ВЫРАВНИВАНИЕ таблиц: широкие reference/matrix-таблицы (инвариант-матрица, сравнения на 4+ колонки)
    выравнивай ячейки пробелами по столбцам (моноширинно) — `|` образуют ровные колонки в исходнике,
    таблица читаема при ревью raw-diff, а не только в рендере. Узкие 2–3-колоночные — по желанию.
    Компромисс: при росте одной ячейки перевыравнивается вся колонка (шумит diff) — это осознанная плата.
  • Диаграммы — ```mermaid``` (GitHub рендерит) вместо абзаца: `flowchart/graph` — поток/зависимости;
    `stateDiagram-v2` — автоматы состояний (статусы, paused-окна, kill-латч); `sequenceDiagram` —
    многоакторные потоки (dual-publish, конвейеры); `timeline` — история версий/инцидентов (ситуативно).
  • Формулы — LaTeX `$…$` / `$$…$$` (GitHub рендерит): `$step=\frac{scale}{\gcd(p,scale)}$` читаемее бэктиков.
  • Сноски `[^имя]` — третий уровень провенанса между инлайном (шумно) и <details> (клик).
  • Кликабельные пути: карта кода («Где живёт») живёт в PLAN; если ссылаешься на файл
    из SPEC — относительной ссылкой `[`x.py`](../../x.py)`. Deep-link на инвариант — якорь `<a id="invN">`.
  • Акценты — GitHub-алерты: `> [!IMPORTANT]` зонтик · `> [!WARNING]` анти-инвариант/футган · `> [!NOTE]` контекст.
  • Многословное «почему»/деталь — под `<details><summary>…</summary>` (клик-раскрытие), спека остаётся тонкой.
  • On-disk / file-tree — box-drawing `├──`/`└──`/`│` внутри ```text; typed-структуры — с разделителями `// ─── секция ───`.
  • Изменение формата (migration / schema-bump) — ```diff-блок (`+`/`-`, красный/зелёный на GitHub).
  • Удалено / устарело / опровергнуто — `~~strikethrough~~` (`~~старое~~ → новое`).
  • Стрелки-словарь: `→` поток/трансформация · `↔` двунаправленно · `⊥` ортогонально/независимо · `⇒` следствие.
  • Emoji — ЗАКРЫТЫЙ СЛОВАРЬ статусов в фиксированной позиции (глиф = один смысл):
    ✅/❌ можно/нельзя · ✓/— тест есть/дыра · ☑/☐ placed/planned · 🔴/🟢 severity/health · ⚠️ футган · 🎯 MVP.
    Декоративные emoji (🚀💡📋…) — НЕТ: плотность убивает салиентность. Ценность ⚠️ — в его редкости.
  • `---` разделяет крупные смысловые половины.

  ЯЗЫК И ЛЕГЕНДА (правило «холодного читателя» действует на ВЕСЬ документ):
  • ПРОЕКТНЫЙ ШИФР (тикет-префиксы вроде COD-N, сокращения вроде D-1/H1/gcd-1) —
    расшифровка в 3–7 слов при ПЕРВОМ употреблении; полный список — в «Глоссарии».
  • МАТ-НОТАЦИЯ (`⊥ ⟺ ∧ ⇒`) в требованиях/инвариантах — либо словами, либо с легендой;
    один символ = ОДИН смысл по всему корпусу.
  • ТЕЛЕГРАФ-ПУНКТ: если пункт требований содержит 3+ независимых проверяемых условия — расщепи.
-->

## Информация о документе

**Status:** [Draft | New (N) | Open (O) | Merged (M) | Shipped (S) | Complete (C) | Closed-no-impl]
**Type:** [Feature (F) | Bug (B) | Refactor (R) | Question/Research (Q)]
**Жанр:** [Продукт (P) | Инженерный (E)]
**Depends on:** [SPEC NNN (роль связи) | —]
**Supersedes / Consolidates:** [SPEC NNN (поглощён сюда) | —]
**Schema bump:** [Не требуется | state.json vN→vN+1 | config format | —]
**Release:** [vX.Y.Z | commit/date-anchor, если тегов нет | не привязано]

> Принцип: **одна фича — один SPEC**. Если задача поглощает более ранние черновики,
> перечисли их в *Consolidates* и не создавай параллельных документов.

---

## Навигация

<!-- Только для длинных спек: якорные ссылки на секции для быстрой навигации.
     Короткую спеку — пропусти целиком. -->

## Введение

### Краткое описание

<!-- 1–3 предложения С ГЛАГОЛОМ (не безглагольный именной фрагмент): ЧТО это за способность,
     какое наблюдаемое поведение появляется. Читается ДО требований и понятно БЕЗ них —
     если описание расшифровывается только через R1–Rn, оно телеграфно → разверни.
     Чистое «что»; «зачем» — в «Ценности» ниже. -->

### Ценность: проблема → решение

<!-- ЗАЧЕМ: что не работает / чего не хватает и ПОЧЕМУ это важно. Начни с боли пользователя (P)
     или инженерного ограничения (E), а не с решения; заверши тем, что решение даёт.
     ХОЛОДНЫЙ ЧИТАТЕЛЬ (обязательное правило этой секции и «Краткого описания»):
     • пиши для того, кто НЕ читал соседних спеков и не может спросить автора;
     • жаргон/доменный термин — расшифровать при ПЕРВОМ употреблении;
     • ключевая механика (почему проблема существует) — словами, не в скобку;
     • тезис ≠ проблема: находка/edge — контекст, а проблема — что без работы сломано/невозможно.
     Тест: человек со стороны пересказывает проблему своими словами? Нет → переписать. -->

### Текущее состояние (до работы)

<!-- Как есть сейчас, по коду: файлы, поведение, ограничение. Точка отсчёта, потом —
     часть исторического контекста. Для жанра E фактически обязательна. -->

### Границы

<!-- Обе половины scope. У Kiro это единая Scope; держи границу вместе с намерением. -->

**В scope:**

- <что сознательно входит в эту задачу>

**Вне scope (non-goals):**

- <что сознательно НЕ делается; вынесенное в будущий SPEC — с предварительным номером>

## Требования

<!-- Формат Kiro: на КАЖДОЕ требование — h3-блок: заголовок + опц. User Story +
     EARS-критерии + детали. User Story обязательна для жанра P; для E — опционально
     («роль» там часто сама система/оператор, ценность — инженерная).
     Per-requirement EARS = приёмка ИМЕННО этого требования; глобальный Definition of Done
     и метрики — в «Критериях успеха». Группируй по подсистемам, если требований много.
     ТЕЛЕГРАФ-ПУНКТ: 3+ независимых условия в одном критерии — расщепи на строки EARS. -->

### R1 — [заголовок]

**User Story:** Как [роль], я хочу [функциональность], чтобы [ценность]. *(P — обязательно; E — опц.)*

#### Критерии приёмки (EARS)

1. WHEN [событие] THEN system SHALL [реакция] — `(test_name)`
2. IF [невалидное/сбой] THEN system SHALL [поведение], NEVER [чего нельзя]
3. WHILE [длящееся состояние] system SHALL [непрерывное поведение]
4. WHERE [контекст/режим] system SHALL [контекстное поведение]

#### Детали

<!-- Опционально. -->
Приоритет [P1|P2|P3] · Сложность [S|M|L] · Зависимости [SPEC NNN / —] · Допущения […]

### R2 — … *(та же структура)*

## Инварианты

<!-- КЛЮЧЕВОЙ durable-контракт LX (v4.3, см. docs/KIRO_ALIGNMENT.md D11/D13). Стоит сразу за Требованиями:
     R = что система делает, INV = что при этом НИКОГДА не ломается; читаются встык.
     SPEC несёт КОНТРАКТ (EARS) + ПРОВЕНАНС (типизированный); enforcement (код·тест·маркер) —
     в PLAN «Инвариант-контроль» (блоками). `analyze` сверяет: у INV есть guard-тест ИЛИ он в «Дырах».
     ФОРМАТ (блок на инвариант, разделитель — строка из `─`, ~40 шт.):
       **INVn — короткое имя.**
       <EARS-контракт — одна строка>
       ↳ **<тип провенанса> (<ссылка>).** <короткое почему; длинный инцидент — в <details>>
     EARS-СЛОВАРЬ: событие → WHEN…THEN system SHALL… · guard → IF…THEN…SHALL…, NEVER… ·
       длящееся → WHILE…system SHALL… · режим → WHERE…system SHALL…
     ТИП ПРОВЕНАНСА (откуда инвариант): **требование (R<n>)** · **принцип (§Const)** ·
       **баг-инцидент (дата)** (выучен сбоем) · **замер** (бэктест/бенчмарк/дебанк). -->

> [!IMPORTANT]
> Зонтик — Constitution §N: <почему способность несёт инварианты — 1 строка>.
> Покрытие: N ✓ · M —. Enforcement (код · тест · маркер) — [plan.md «Инвариант-контроль»](plan.md).

**INV1 — <короткое имя>.**
WHEN <триггер> THEN system SHALL <обязательство>.
↳ **требование (R1).** <почему существует; что защищает; цена нарушения>.
────────────────────────────────────────
**INV2 — <имя guard-инварианта>.**
IF <невалидный вход/сбой> THEN system SHALL <поведение>, NEVER <чего нельзя>.
↳ **баг-инцидент (YYYY-MM-DD).** <что сломалось — 1 строка>.
<details><summary>детали инцидента/замера</summary>

<история прозой; точный коммит — `git blame file:symbol`>
</details>

### Дыры покрытия (тест: —)

<!-- Инварианты без реального теста — вход для `/speckit.trace --stubs`. EARS-контракт = ТЗ заглушки.
     ЗАКРЫТЫЙ СЛОВАРЬ ТИПОВ ДЫР (v4.4, лестница — ENFORCEMENT_LADDER): дыра ≠ дыра, тип указывается явно:
       • тест planned — появится с реализацией (кандидат-имя обязательно);
       • процедура — ревью/host-smoke, осознанный потолок (когда выше подняться нельзя);
       • косвенный guard — защищено побочно (именованного теста нет; чем именно — словами).
     trace различает типы, а не сыплет одинаковые WARN. -->

- **INVn** — тест planned: <что проверить> → кандидат `test_<inv>`.
- **INVm** — процедура: <чем закрыто — ревью/host-smoke и почему выше нельзя>.

### Анти-инварианты (что НЕЛЬЗЯ)

> [!WARNING]
> <футган: соблазнительное изменение X> нарушит **INVn**: <последствие>.

## Нефункциональные требования

<!-- Опционально: производительность · безопасность · удобство · надёжность.
     Формулируй В EARS (как Kiro), каждое измеримо: WHILE [нагрузка] system SHALL [порог].
     Пиши только реально применимые. -->

## Ограничения и допущения

<!-- Опционально: ВНЕШНИЕ рамки — технические/бизнес-ограничения и допущения, при которых
     верна спека (внешние API, платформенные лимиты, предположения о нагрузке/окружении).
     Не путать с инвариантами: инвариант — внутреннее обязательство, ограничение — внешняя рамка. -->

## Риски

| Риск | Вероятность/эффект | Смягчение |
|------|--------------------|-----------|
|      |                    |           |

## Критерии успеха

<!-- Kiro Success Criteria = ГЛОБАЛЬНЫЙ rollup, не per-requirement (те EARS — у каждого R).
     «быстро/удобно/корректно» без числа/порога — не метрика. -->

### Definition of Done

- [ ] Все per-requirement EARS-критерии (см. «Требования») выполнены и покрыты тестами
- [ ] Сборка/тесты/анализ зелёные — одной командой (см. checklist)
- [ ] Инварианты INV1–INVn сохранены и подтверждены (guard-тест из «Инвариантов» / host-smoke)

### Метрики приёмки

<!-- Опционально: измеримые исходы приёмки — SHALL с числом/порогом. -->

- <метрика: SHALL ≥/≤ порог>.

## Отклонённые альтернативы / Что НЕ сделано

<!-- Самое долговечное знание — то, чего в коде НЕТ: что пробовали и почему отказались,
     что сознательно отложили. Выноси на первый план, а не прячь. Пропусти, если отвергать было нечего.
     Несущие design-решения (Context/Options/Decision) — в PLAN «Ключевые решения»; сюда — их вывод. -->

- **Отвергнуто:** <подход> — почему проиграл (замер/инцидент), чем заменён.
- **Отложено сознательно:** <что> — причина и условие возврата (→ SPEC NNN, если вынесено).

## Связи (трассируемость)

<!-- Двунаправленные ссылки. Роли связи различаются явно.
     Second Brain: долговечные уроки, переживающие задачу, линкуй в базу знаний через `[[note-slug]]`. -->

- **Зависит от:** SPEC NNN — …
- **Порождает:** будущий SPEC (предв. номер …) — …
- **Референс:** SPEC NNN / docs/… — …
- **План (дизайн):** [`plan.md`](plan.md) *(инварианты — в секции «Инварианты» выше)*
- **Память (Second Brain):** `[[note-slug]]`, `[[…]]` — долговечные памятки/уроки.

## Глоссарий

<!-- Условно-обязателен, если в спеке есть проектный шифр/жаргон: собери сюда расшифровки,
     данные инлайн при первом употреблении, — чтобы спека читалась холодным читателем. -->

## Чек-лист ревью требований

<!-- Опционально: полный чек-лист качества спеки — во внешнем checklist-template.md.
     Здесь достаточно ссылки или короткой сверки перед переходом к /speckit.plan. -->
