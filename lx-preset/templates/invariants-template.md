# INVARIANTS [NNN] — [SHORT_TITLE]

<!--
  LX invariants — ДОМ инвариантов (durable-технический контракт-слой), а не разгрузочный файл.
  Инвариант — техника («как не сломать»), не бизнес → не в requirements-теле SPEC; и durable
  (переживает redesign) → не в эфемерном PLAN. Дом — здесь (см. docs/KIRO_ALIGNMENT.md, D4).
  Порога нет: дом инвариантов независимо от их числа. Есть хоть один инвариант — он живёт здесь
  (в SPEC — только строка-ссылка + индекс; прежний порог ≥3 снят, засорять SPEC больше нечем).
  SPEC держит ТОЛЬКО строку-ссылку + терсный индекс утверждений; enforcement (где маркер, каким
  тестом) — в PLAN §14 «Инвариант-контроль». `analyze` сверяет три точки: SPEC ↔ invariants.md ↔ PLAN.
  Пустой `тест:` (`—`) = дыра покрытия. `git blame Код` даёт коммит (хешей не держим).
  Каждый инвариант, затрагивающий код, несёт парный маркер `// SPEC NNN invariant: …`.

  EARS-СЛОВАРЬ для контракта (заимствовано из Kiro/EARS — форсит проверяемость):
    событие        → WHEN  [триггер]            THEN system SHALL [обязательство]
    guard/ошибка   → IF    [невалидное/сбой]    THEN system SHALL [поведение]
    непрерывное    → WHILE [длящееся условие]        system SHALL [поведение]
    режим/контекст → WHERE [конфиг/платформа]        system SHALL [поведение]
    запрет         → … SHALL …, NEVER [чего делать нельзя]
  Контракт в EARS = готовый arrange/act/assert будущего теста.
-->

## 1. Информация о документе

**Spec:** [NNN] · **Status:** as-built / current · **Покрытие:** N ✓ · M — · **Зонтик:** Constitution §… (почему способность несёт инварианты)

<!-- «Покрытие» — светофор одним взглядом: сколько инвариантов защищено тестом (✓) и сколько дыр (—). -->

## 2. Тест-матрица (сводка)

<!-- Обзор одним взглядом: краткое УТВЕРЖДЕНИЕ + провенанс + статус. Source of truth — записи в §3
     (там полный EARS-контракт и детали). ВЫРАВНИВАЙ ячейки моноширинно (см. визуальные конвенции
     spec-template): широкая matrix-таблица должна читаться в raw-diff. Deep-link из PR — якорь
     `<a id="invN"></a>` в §3. -->

| INV  | Утверждение                               | Код                     | Тест                       | Провенанс (кратко)               | Маркер    |
|------|-------------------------------------------|-------------------------|----------------------------|----------------------------------|-----------|
| INV1 | parity: `logic.py` бит-в-бит легаси       | `logic.py` (шапка)      | `test_logic_parity`        | порт verbatim; иначе замеры мимо | ✓ placed  |
| INV2 | вселенная = allowlist (12 EU/US)          | `config.py:ALLOWLIST`   | `test_allowlist_is_twelve` | инцидент 2026-07-02: Азия −$80   | ✓ placed  |
| INV3 | guard: IF невалидно THEN skip, NEVER краш | `logic.py:apply_guards` | —                          | инцидент: пачка обошла лимит     | ☐ planned |

## 3. Записи инвариантов

<!-- ТРЁХСТРОЧНЫЙ формат записи:
       1) короткое ИМЯ жирным + инлайн-статус маркера (`✓placed`/`☐planned`) — идёт в индекс spec.md;
       2) КОНТРАКТ в EARS-форме (словарь выше) — исполняемая формулировка для теста;
       3) привязки: `код · тест · провенанс · §зонтик`.
     Провенанс: `R<n>` (design) · `§<Const>` (архитектурный) · `инцидент: <строка>` (выучен сбоем). БЕЗ git-хешей.
     ТЕСТ-ПОЛЕ: имя `test_…` (grep-able) или честное `—` (host/нет) — НИКОГДА описание
     («тесты отбора», «легаси-тесты») — его не найти grep'ом и не проверить trace'ом.
     Длинный инцидент/замер — под <details> у записи. Пустая строка между записями. -->

- **INV1 — <короткое имя; читается само>.** ✓placed
  WHEN <триггер> THEN system SHALL <обязательство>.
  ↳ `file:symbol` · `test_name` · §Const…

- **INV2 — <имя guard-инварианта>.** ☐planned
  IF <невалидный вход/сбой> THEN system SHALL <поведение>, NEVER <тихий дефолт/падение>.
  ↳ `file:symbol` · тест: — · инцидент: <что сломалось, 1 строка> · §…
  <details><summary>детали инцидента</summary>

  <история/замер прозой; точный коммит — `git blame file:symbol`>
  </details>

- **INV3 — <имя режимного инварианта>.** ✓placed
  WHERE <режим/платформа> system SHALL <поведение>; WHERE <иной режим> — SHALL <иное>.
  ↳ `file:symbol` · `test_name` · почему: R3 · §…

## 4. Дыры покрытия (тест: —)

<!-- Инварианты без реального теста — вход для `/speckit.lx.trace --stubs`.
     EARS-контракт записи = готовое ТЗ заглушки (WHEN/IF → arrange/act; SHALL → assert). -->

- **INVn** — нет теста: <почему трудно / что проверить> → кандидат в `test_<inv>`.

## 5. Анти-инварианты (что НЕЛЬЗЯ)

<!-- Футганы: соблазнительные изменения, нарушающие инвариант. -->

> [!WARNING]
> <откат/изменение X> нарушит **INVn**: <последствие>.
