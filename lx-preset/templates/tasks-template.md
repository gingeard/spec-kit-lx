# TASKS [NNN] — [SHORT_TITLE]

<!--
  LX TASKS. Заполняется командой /speckit.tasks из PLAN.
  Чеклист = и план, и трекер факта: каждый пункт отражает реальное состояние ([ ]/[x]).
  Здесь же живёт SEQUENCING (какие файлы менять, в каком порядке) — пришло из PLAN
  (см. docs/KIRO_ALIGNMENT.md, D3): PLAN отвечает «как устроено», TASKS — «в каком порядке строим».
  Группировка по фазам/подсистемам. Формат пункта: [ ] [P?] описание — файл.
  [P] = можно делать параллельно (нет общего состояния с соседями по фазе).
-->

**Spec:** [NNN] · **Plan:** готов

## Изменяемые файлы

<!-- Таблица «файл | функция(участок) | что делать». Ничего вне этого списка агент
     не трогает без явного согласования (scope-контроль). Пришло из PLAN. -->

| Файл | Функция / участок | Что делать |
|------|-------------------|------------|
|      |                   |            |

## Новые файлы

| Файл | Назначение |
|------|------------|
|      |            |

## Фаза 1 — [подсистема/данные]

- [ ] …  — `path/to/file`
- [ ] [P] … — `path/to/file`

## Фаза 2 — [логика]

- [ ] …

## Фаза 3 — [интеграция]

- [ ] …

## Тесты и проверки

- [ ] Unit: happy path + 1–2 edge + невалидный ввод (детерминированные, без сети/времени/ФС)
- [ ] `go build ./...` · `go test ./...` · `go vet ./...` — зелёные
- [ ] Доменная проверка (если применимо), напр. валидатор внешнего формата — OK

## Закрытие (обязательная фаза)

<!--
  Ритуал закрытия задачи. Не пропускается. Дублирует Definition of Done + требования
  AGENTS/README к документации и статусу. Автоматизируется командой
  /speckit.lx.close (расширение lx-flow-ext).
-->

- [ ] Инварианты (SPEC «Инварианты») подтверждены guard-тестами `test_specNNN_invM_*`; матрица без ☐planned/дыр
- [ ] Заполнен `implementation_report.md` (итог, изменения, решения, проверки, безопасность)
- [ ] Обновлён `docs/release_notes/upcoming.md` (EN Highlights + RU Основное) со ссылкой на SPEC [NNN]
- [ ] При изменении архитектуры/потоков — обновлён `docs/ARCHITECTURE.md`
- [ ] Трассировка проверена: код ↔ SPEC, SPEC ↔ SPEC (Depends on)
- [ ] Статус SPEC переведён во front-matter: `Status: [M|S|C]` (+ Release: vX.Y.Z, если привязано)
