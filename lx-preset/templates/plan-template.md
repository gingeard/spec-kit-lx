# PLAN [NNN] — [SHORT_TITLE]

<!--
  LX PLAN. Заполняется командой /speckit.plan из SPEC.
  PLAN отвечает на «КАК строить»: конкретные файлы, решения, порядок.
  Минимальный дифф: план меняет только то, что покрыто SPEC.
-->

**Spec:** [NNN] · **Constitution:** проверено (см. Constitution Check) · **Дата:** [YYYY-MM-DD]

## Резюме

<!-- 2–4 предложения: как достигается цель SPEC технически. -->

## Изменяемые файлы

<!-- Таблица «файл | функция(строки) | что делать». Ничего вне этого списка агент
     не трогает без явного согласования (scope-контроль). -->

| Файл | Функция / участок | Что делать |
|------|-------------------|------------|
|      |                   |            |

## Новые файлы

| Файл | Назначение |
|------|-----------|
|      |           |

## Ключевые решения

<!-- «Думай данными, а не кодом»: сначала опиши структуру данных, потом код вокруг неё.
     ДВА УРОВНЯ записи (по весу решения):
     • МЕЛКИЙ выбор → однострочник (как ниже);
     • НЕСУЩЕЕ решение (движок/схема/протокол; обычно 1–3 на план) → Decision-блок
       (формат Kiro Decision Documentation): Context → Options (таблица с минусами!) →
       Decision → Rationale → Implications. Ценность — в отвергнутых опциях и последствиях:
       это знание, которого в коде нет (принцип #7). Implications часто рождают инварианты;
       отвергнутые опции при закрытии переезжают в «Отклонённые альтернативы» спеки. -->

- Решение: … · Альтернатива: … · Почему: …

### D1 — <несущее решение: что выбираем> (опционально, для 1–3 решений)

**Context:** <вводные, при которых решали — 1-2 строки>

**Options:**
| Опция | + | − | Effort |
|---|---|---|---|
| A. <вариант> | … | … | … |
| B. <вариант> | … | … | … |

**Decision:** <A/B и суть>.
**Rationale:** <почему победила; чем хеджирован риск>.
**Implications:** <что теперь зависит от выбора; что делать при провале; кандидаты в INV>.

## Constitution Check

<!-- Явная сверка с SPECS/CONSTITUTION.md перед реализацией. Каждый пункт — PASS/риск. -->

- [ ] Архитектурные инварианты не нарушены (слои/зависимости, детерминизм парсера, изоляция платформы)
- [ ] Запреты соблюдены (нет хардкода секретов, нет прямых UI-вызовов из логики, ошибки не игнорируются)
- [ ] Логирование в ключевых точках (start/success/error)
- [ ] Обратная совместимость: функциональная сохранена; schema bump заявлен в SPEC при необходимости

## Фазы реализации

<!-- Порядок работ. ИНВАРИАНТ ПРОЦЕССА: каждая фаза оставляет ветку зелёной
     (go build/test/vet). Порядок обычно: данные/типы → логика → интеграция → тесты. -->

1. Фаза 1 — … (оставляет ветку зелёной)
2. Фаза 2 — …

## Инвариант-контроль

<!-- Как в коде будут проставлены маркеры `// SPEC [NNN] invariant: …` и где именно. -->

- …
