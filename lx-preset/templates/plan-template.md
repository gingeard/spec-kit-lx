# PLAN [NNN] — [SHORT_TITLE]

<!--
  LX PLAN. Заполняется командой /speckit.plan из SPEC.
  PLAN отвечает на «КАК устроено решение»: архитектура, компоненты, модель данных, решения.
  По содержанию это Kiro design.md; имя артефакта (`plan`) держим ради совместимости с ядром
  Spec Kit (команда/хуки/analyze), см. docs/KIRO_ALIGNMENT.md (D1). Раскладка секций — по Kiro design.md.
  Sequencing (какие файлы менять, в каком порядке, фазы) — НЕ здесь, а в tasks.md.
  Визуальные конвенции — те же, что в spec-template.md (mermaid, fenced-блоки, EARS, стрелки-словарь).
  Минимальный дифф: дизайн покрывает только то, что заявлено в SPEC.
-->

## 1. Информация о документе

**Spec:** [NNN] · **Constitution:** проверено (см. §13) · **Дата:** [YYYY-MM-DD]

## 2. Обзор

<!-- 2–4 предложения: как достигается цель SPEC технически, на уровне «как устроено». -->

### 2.1 Цели дизайна

<!-- Опционально: что оптимизируем (простота/скорость/обратная совместимость/тестируемость)
     и какими компромиссами платим. -->

### 2.2 Ключевые решения

<!-- «Думай данными, а не кодом»: сначала структура данных, потом код вокруг неё.
     ДВА УРОВНЯ записи (по весу решения):
     • МЕЛКИЙ выбор → однострочник (как ниже);
     • НЕСУЩЕЕ решение (движок/схема/протокол; обычно 1–3) → Decision-блок (формат Kiro):
       Context → Options (таблица с минусами!) → Decision → Rationale → Implications.
     Ценность — в отвергнутых опциях и последствиях: это знание, которого в коде нет.
     Implications часто рождают инварианты (→ invariants.md); отвергнутые опции при закрытии
     переезжают в SPEC §Отклонённые альтернативы. -->

- Решение: … · Альтернатива: … · Почему: …

#### D1 — <несущее решение: что выбираем> (опционально, для 1–3 решений)

**Context:** <вводные, при которых решали — 1–2 строки>

**Options:**

| Опция        | + | − | Effort |
|--------------|---|---|--------|
| A. <вариант> | … | … | …      |
| B. <вариант> | … | … | …      |

**Decision:** <A/B и суть>.
**Rationale:** <почему победила; чем хеджирован риск>.
**Implications:** <что теперь зависит от выбора; что делать при провале; кандидаты в INV>.

## 3. Архитектура

<!-- Опционально, для нетривиальных задач. Мелкой/ретро-спеке достаточно §4 «Где живёт». -->

### 3.1 Контекст системы

<!-- Где способность в системе; границы. ```mermaid flowchart``` вместо абзаца. -->

### 3.2 Высокоуровневая архитектура

<!-- Блоки решения и потоки данных между ними. -->

### 3.3 Технологический стек

<!-- Только если есть выбор: что используем и почему. -->

## 4. Где живёт (карта кода)

<!-- As-built навигация: файл · символ · роль. Точка входа для будущих правок.
     Пришло из SPEC (см. KIRO_ALIGNMENT.md): дизайн-контент живёт в PLAN, не в requirements. -->

| Файл                                 | Символ      | Роль |
|--------------------------------------|-------------|------|
| [`path/to/file`](../../path/to/file) | `func/type` | …    |

## 5. Модель данных

<!-- Условно-обязательна, если способность — источник истины для формы данных
     (тип на шине, схема файла состояния, on-disk формат). Покажи ФОРМУ, а не прозу. -->

**Тип / схема** (поле · тип · семантика):

```text
TypeName{ field: type — что это; ts_event=… ; ts_init=… ; … }
```

**Раскладка на диске** (для data-heavy — где и по какому ключу лежит + размерная математика):

```text
{root}/.../{id}/*.ext        — что лежит
порог ротации / retention → on-disk-эффект (напр. 3 MB counted ≈ 27 MB диск)
```

## 6. Контракты / API

<!-- Условно-обязательна, если есть внешний контракт: RPC, HTTP-эндпоинт, шина команд.
     Сигнатуры и семантика полей — fenced-блоком. -->

## 7. Безопасность

<!-- Опционально: аутентификация/авторизация, валидация входа, защита данных — если применимо. -->

## 8. Обработка ошибок

<!-- Опционально: классы ошибок, fail-fast при старте, поведение при откате/деградации. -->

## 9. Производительность

<!-- Опционально: ожидаемая нагрузка, узкие места, размерная математика, стратегия оптимизации. -->

## 10. Тестирование

<!-- Опционально: стратегия проверки дизайна — unit/integration/e2e/host-smoke.
     Конкретные критерии «сделано» — в SPEC §Критерии; здесь — как именно проверяем. -->

## 11. Развёртывание

<!-- Опционально: сборка/релиз/конфигурация/мониторинг, если задача этого касается. -->

## 12. Миграция и совместимость

<!-- Обязательна, если во front-matter SPEC задан `Schema bump`. -->

- **Направление:** одно-/двунаправленно; поведение при откате (downgrade → regenerate / ошибка / …).
- **Старые артефакты:** что с ними (конвертация/дроп/lazy-GC), **кто** делает и **когда** (старт/миграция).

## 13. Сверка с конституцией

<!-- Явная сверка с SPECS/CONSTITUTION.md перед реализацией. Каждый пункт — PASS/риск. -->

- [ ] Архитектурные инварианты не нарушены (слои/зависимости, детерминизм парсера, изоляция платформы)
- [ ] Запреты соблюдены (нет хардкода секретов, нет прямых UI-вызовов из логики, ошибки не игнорируются)
- [ ] Логирование в ключевых точках (start/success/error)
- [ ] Обратная совместимость: функциональная сохранена; schema bump заявлен в SPEC при необходимости

## 14. Инвариант-контроль

<!-- Как ЭТОТ дизайн держит инварианты из invariants.md: где в коде проставляются маркеры
     `// SPEC [NNN] invariant: …` и каким тестом защищён каждый. Enforcement живёт здесь;
     утверждение и durable-запись — в invariants.md. Держи в синхроне (`analyze` сверяет). -->

| INV  | Где ставится маркер (`file:symbol`) | Тест        | Статус               |
|------|-------------------------------------|-------------|----------------------|
| INV1 | `path:sym`                          | `test_name` | ☐ planned → ☑ placed |

## 15. Чек-лист ревью дизайна

<!-- Опционально: полнота дизайна перед /speckit.tasks — покрыты ли все требования SPEC,
     нет ли нерешённых Decision-блоков, заявлена ли миграция при schema bump. -->
