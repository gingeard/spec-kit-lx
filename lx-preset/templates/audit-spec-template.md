# AUDIT [NNN] — [SCOPE_TITLE]

<!--
  LX AUDIT-SPEC (жанр Q/аудит; образец — sing-box-lx/022-LX_DEEP_AUDIT).
  Не «что построить», а «что не так и что здорово» в заданной зоне.
  Проза — русский; идентификаторы/пути — как в коде.
  ГЛАВНОЕ ОТЛИЧИЕ от обычной спеки: findings ранжированы по severity, каждый
  привязан к `file:line` и к SPEC-владельцу; и обязателен раздел «Опровергнуто
  верификацией» — findings, которые при проверке оказались НЕ дефектами
  (adversarial-проверка своих же выводов). Читабельность — как в spec-template.
-->

**Status:** [In-progress | Complete (C)]
**Type:** Q (Audit/Research)
**Scope:** [зона: пакеты/подсистемы/диапазон файлов]
**База:** [ревизия/тег/ветка, на которой проведён аудит]
**Метод:** [чтение + прогон тестов + инструментализация/probe + …]

---

## Сводка по severity

| Уровень | Кол-во | Что это |
|---------|--------|---------|
| **P0** | 0 | ломает стабильность в проде — исправить немедленно |
| **P1** | 0 | корректность/безопасность состояния |
| **P2** | 0 | узкие/косметические баги, наблюдаемость, документация |
| **P3** | 0 | nit: гигиена, мёртвый код, микро-оптимизации, маркеры |
| Опровергнуто | 0 | подозрения, снятые верификацией (НЕ дефекты) |

### По осям

<!-- Разрез находок по классам: логика · состояние/гонки · безопасность · производительность ·
     наблюдаемость · документация. Даёт картину «где болит», а не только счёт. -->

- логика: … · состояние/гонки: … · безопасность: … · производительность: … · наблюдаемость/доки: …

---

## P0 — исправить (стабильность в проде)

<!-- Формат заголовка находки (сканируется): #N · SEVERITY · класс · `file:line` · SPEC NNN.
     Тело: что не так → почему это дефект (сценарий отказа) → действие/фикс. -->

### #1 · HIGH · логика · `path/file.go:LINE` · SPEC NNN
- **Что:** …
- **Почему дефект:** конкретный сценарий → неверный результат/краш.
- **Действие:** … (фикс / отдельный SPEC / маркер).

## P1 — исправить (корректность/безопасность состояния)

### #2 · MEDIUM · … · `path:LINE` · SPEC NNN
- **Что / Почему / Действие:** …

## P2 — low (узкие баги, наблюдаемость, документация)

### #3 · LOW · … · `path:LINE` · SPEC NNN
- …

## P3 — nit (гигиена)

- `path:LINE` — комментарий/мёртвый код/недостающий маркер `// SPEC NNN` · …

---

## Опровергнуто верификацией (НЕ дефекты)

<!-- ОБЯЗАТЕЛЬНЫЙ раздел. Каждое подозрение, которое ты проверил и снял — с ДОКАЗАТЕЛЬСТВОМ,
     почему это не баг (тест/чтение кода/замер). Это защита от «правдоподобного мусора»:
     аудит без этого раздела подозрителен — значит findings не верифицировались. -->

- **~~<подозрение>~~** — снято: <доказательство> (`file:line` / тест `name` / замер / `[git]`).

## Что аудит подтвердил как здоровое (baseline)

<!-- Явно назови узлы, которые проверены и корректны — чтобы будущий аудит не перепроверял
     и чтобы «тишина» не читалась как «не смотрели». -->

- `<подсистема>` — инвариант(ы) держатся; покрыто `test_…`. …

## Рекомендация по приоритизации

<!-- Что делать в каком порядке; какие находки блокируют релиз, какие можно отложить. -->

1. …

---

## Связи (трассируемость)

- **Аудирует:** SPEC NNN, NNN … (владельцы зон, куда уходят findings).
- **Порождает:** SPEC NNN (фикс P0/P1) / follow-up.
- **Память (Second Brain):** `[[note-slug]]` — уроки аудита, переживающие эту задачу.
- **Код:** места findings несут (или получат) маркер `// SPEC NNN` при фиксе.
