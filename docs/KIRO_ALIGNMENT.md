# Выравнивание шаблонов LX по Kiro: SPEC / PLAN / invariants

Ресёрч-документ: как перестроить шаблоны LX (`spec` / `plan` / `invariants`),
максимально сблизив их с трёхфазной моделью Kiro (`requirements` → `design` → `tasks`),
**сохранив** то, что есть в LX, и **добавив** то, что есть в Kiro, но отсутствует в LX.

Принцип переноса: `requirements ↔ spec`, `design ↔ plan`, `tasks ↔ tasks` — слоты уже
совпадают 1:1, поэтому это не перестройка конвейера, а **переименование секций +
перебалансировка содержимого** внутри уже существующих артефактов.

Легенда атрибутов: **О** — обязательная секция · **о** — опциональная · **У** — условная
(обязательна при выполнении условия). `(—)` в колонке Kiro — секция LX-native, прямого
аналога у Kiro нет (в скобках — ближайший по смыслу слот).

---

## 1. Отправная точка: три корпуса

| Модель | Артефакты на фичу | Ось «ЧТО/ЗАЧЕМ ↔ КАК» |
|--------|-------------------|------------------------|
| GitHub Spec Kit | 1 × `spec.md` + `plan.md` + `tasks.md` | spec = ЧТО/ЗАЧЕМ, plan = КАК |
| Kiro | `requirements.md` + `design.md` + `tasks.md` | req = продукт/бизнес, design = инженерия |
| LX (текущий) | `SPEC.md` (+ `invariants.md`) + `PLAN.md` + `TASKS.md` | SPEC = ЧТО/ЗАЧЕМ, PLAN = КАК |
| Leadaxe (первоисточник) | `SPEC.md` (freeform) + опц. `PLAN`/`TASKS` | SPEC = живой документ, источник инвариантов |

**Ключевое наблюдение.** Конвейер Spec Kit `spec → plan → tasks → implement` почти
1:1 ложится на Kiro `requirements → design → tasks`. `plan` уже занимает слот `design` —
меняется только имя-акцент, не порядок фаз (порядок фаз в Spec Kit расширяется hooks,
но не переставляется — совпадает с Leadaxe).

---

## 2. Ключевые решения этого ресёрча

| # | Решение | Обоснование |
|---|---------|-------------|
| D1 | **Имя `plan.md` / `/speckit.plan` держим**, меняем только содержание → Kiro-`design` | Ядро Spec Kit владеет командой/артефактом; переименование форкает naming-конвенцию, хуки, `analyze`. Ре-скоуп даёт 95% выгоды за 5% цены. «PLAN по имени — DESIGN по сути». |
| D2 | **Дизайн-секции уходят из SPEC в PLAN**: `Где живёт`, `Схема данных` | Это as-built КАК (Components / Data Models / API у Kiro), не требования. SPEC очищается до чистого requirements-документа. |
| D3 | **Sequencing уходит из PLAN в TASKS**: `Изменяемые/Новые файлы`, `Фазы реализации` | Это планирование работ (Kiro `tasks.md › Implementation Plan`), дублировало `tasks.md`. |
| D4 | **Инварианты — целиком в `invariants.md`** (durable-технический дом); из SPEC уходят, остаётся строка-ссылка | Инвариант — по оси предметной области **техника** («как не сломать»), не бизнес → не в бизнес-SPEC. По оси долговечности — **durable** → не в эфемерном PLAN. `invariants.md` удовлетворяет **обе** оси. |
| D5 | **Порога для `invariants.md` нет** — дом инвариантов независимо от их числа | Прежний порог `≥3` существовал, чтобы не засорять SPEC. Раз инвариантов в SPEC больше нет — порог не нужен: есть хоть один инвариант, он живёт в `invariants.md`. |
| D6 | **Режим «оба жанра»**: маркер `Жанр: Продукт (P) \| Инженерный (E)` + жанро-нейтральные секции | LX/Leadaxe — преимущественно systems-жанр (там «требование» само по себе инженерное), но целимся и в продуктовые фичи. Маркер сообщает читателю/тулингу оптику; для E-спек SPEC честно допускаем тонким (центр тяжести — PLAN + `invariants.md`). |
| D7 | **Порядок секций — Kiro-канон** | Introduction: Feature Summary → Business Value → Scope. Design: Security → Error Handling, Deployment → Migration. LX-native секции без Kiro-дома тукаются в конец соответствующего блока. |
| D8 | **`checklist` ≠ `invariants`** — не заменять одно другим | Ортогональны: checklist — эфемерный DoD-**гейт** (generic, галочка, протыкается раз перед закрытием); invariants — durable **контракт** (уникальная структурная запись). Свернуть инвариант в галочку = выбросить утверждение/провенанс/привязку. Связь: инвариантную секцию checklist'а **порождать** из `invariants.md` (гейт из источника истины). |
| D9 | **Ближайший родственник инварианта в ядре Spec Kit — Constitution**; `invariants.md` закрывает реальную дыру | Прочёсывание spec-kit: точного аналога нет. По духу (durable/непреложное) ближе всех Constitution — но это **системная** половина (широкие принципы, без привязки `file:symbol`/тест/маркер). Локальной половины (точечный код+тест-привязанный контракт) в ядре нет — её и заполняет `invariants.md`. Совпадает с делением LX: системные инварианты → Constitution §2, локальные → `invariants.md`. |
| D10 | **v4.1 — Kiro-требования и снятие нумерации** (см. §10) | Per-requirement блоки как у Kiro (`### Rn` + User Story + EARS-приёмка + Детали); «Критерии успеха» = глобальный DoD + Метрики; Инварианты — сразу после Требований (R = что делает, INV = что не ломается — читаются встык); **нумерация секций снята**: опциональные секции опускаются по-разному → «§9» неоднозначен на уровне корпуса; точную адресацию несут ID-сущности (Rn, INVn, Dn) + имена секций. Частично пересматривает D7. |
| D11 | **v4.2 — `invariants.md` слит в SPEC** (разворот D4/D5) | Отдельный файл держался ТОЛЬКО на «SPEC = бизнес-документ»; но 17/18 спек корпуса — жанр E, где инвариант = сердце спеки, а ось долговечности файл не оправдывала (SPEC так же durable, как файл — она исключала лишь PLAN). Файл давал тройное дублирование (SPEC-индекс + матрица + PLAN-таблица) и дрейф статусов (002 INV1: ✓ vs 🔴) → вечный налог на `analyze`. Слияние: секция `## Инварианты` в SPEC = единый дом (матрица · записи · дыры · анти-инварианты); PLAN «Инвариант-контроль» — только forward-жанр; enforcement — guard-тесты (D14). Возврат к Leadaxe: «SPEC — источник инвариантов» (файл был нашим изобретением при ≥3). Хвост: `/speckit.trace`, `/speckit.retrospec`, `spec-map.sh` перенаправить на секцию SPEC. |
| D12 | **Формат таблица vs блоки — по ширине строки** | Выровненную моноширинную таблицу держим, если строка умещается (~≤100 симв.); при длинных `test_name`/путях за 100 → БЛОЧНЫЙ формат (`label: value` + `────`-разделители). Сужает прежнее «выравнивай широкие таблицы»: широкое ≠ таблица. Reflow-тул выбирает table/blocks автоматически по замеру ширины. |
| D13 | **v4.3 — инвариант = EARS-контракт + типизированный провенанс; enforcement в PLAN** | SPEC «Инварианты»: блок на INV (имя · EARS-контракт `WHEN/IF/WHILE/WHERE … SHALL` · `↳ тип (ссылка)`); тип провенанса — `требование (Rn)` / `принцип (§Const)` / `баг-инцидент (дата)` / `замер`. `Матрица`+`Записи` убраны: EARS-контракт — единственный носитель утверждения, дубля нет. Enforcement (код·тест·маркер) уехал в PLAN «Инвариант-контроль» блоками. Уточняет D11 (там enforcement был в SPEC): SPEC чист (контракт+почему), PLAN держит «где закреплено». |
| D14 | **In-code маркер-комментарии `// SPEC NNN invariant:` упразднены** (не тег, не прозу) | 159 комментариев в коде дублировали SPEC-контракт + PLAN «Инвариант-контроль» (Код: file:symbol) + имена guard-тестов; ни один guard-тест на них не якорился (цепляются за символы/литералы/поведение). Enforcement = guard-тест `test_specNNN_invM_*`; связь код↔спека = PLAN.Код + имя теста. Шаблоны/команды (implement/retrospec/close/checklist/constitution/audit) переориентированы: в код прозу-маркеры НЕ писать. |

> [!IMPORTANT]
> **Актуальный формат — v4.3 (D10–D14).** Решения D4/D5 и целевые скелеты в §3–§6 ниже
> описывают более раннюю v4 (инварианты в отдельном `invariants.md`, нумерованные секции).
> Инварианты живут секцией `## Инварианты` внутри SPEC (EARS-контракт + провенанс), enforcement — в PLAN; секции не нумеруются; широкие таблицы — блоками (D12).
> D4/D5 сохранены как история решения; читать их через призму D11.

### Разбор D4 подробнее: инвариант по двум осям

Инвариант — гибрид, и его надо резать не по одному, а по обоим измерениям одновременно:

| Часть инварианта | Ось: предметная область | Ось: долговечность | Дом |
|------------------|-------------------------|--------------------|-----|
| Утверждение («guard всегда в начале») + §Const + почему | техника | durable (переживает redesign) | `invariants.md` |
| Привязка (`file:symbol` · `test` · маркер · дыры) | техника/impl | ephemeral (умирает с реализацией) | `invariants.md` (запись) + PLAN §Инвариант-контроль (как ЭТОТ дизайн держит) |

Отвергнутые альтернативы:
- **Оставить утверждение в SPEC** («разрез по шву») — держалось на допущении «SPEC = инженерные requirements». В режиме «оба жанра» для продуктового SPEC техническому утверждению не место.
- **Утащить инвариант целиком в PLAN** — ломает долговечность: PLAN эфемерен, при supersede плана утверждение умрёт. Инвариант — самое долговечное знание проекта.

---

## 3. Целевой SPEC (requirements)

| # | Секция | Оригинал Kiro | Атрибут |
|---|--------|---------------|---------|
| 1 | Информация о документе (+ поле `Жанр: P\|E`) | *Document Information* | **О** |
| 2 | Навигация | *Quick Navigation* | о |
| 3 | **Введение** | *Introduction* | **О** |
| 3.1 | ⟶ Цель | *Feature Summary* | **О** |
| 3.2 | ⟶ Проблема | *Business Value* | **О** |
| 3.3 | ⟶ Текущее состояние | *(— baseline)* | о |
| 3.4 | ⟶ Что входит | *Scope (in)* | **О** |
| 3.5 | ⟶ Вне scope | *Scope (out) / Non-goals* | **О** |
| 4 | Требования | *Requirements* | **О** |
| 5 | Нефункциональные требования | *Non-Functional Requirements* | о |
| 6 | Инварианты (строка-ссылка на `invariants.md`) | *(→ Constraints)* | **У** |
| 7 | Ограничения и допущения | *Constraints and Assumptions* | о |
| 8 | Риски | *(→ Assumptions)* | о |
| 9 | Критерии приёмки | *Success Criteria* | **О** |
| 10 | Отклонённые альтернативы | *(→ Key Design Decisions)* | о |
| 11 | Связи | *(→ Requirements Traceability)* | о |
| 12 | Глоссарий | *Glossary* | **У** |
| 13 | Чек-лист ревью | *Requirements Review Checklist* | о |

Уходит из SPEC: `Где живёт`, `Схема данных` → PLAN (D2); тело инвариантов → `invariants.md` (D4).

---

## 4. Целевой PLAN (design по содержанию, имя `plan` — D1)

| # | Секция | Оригинал Kiro | Атрибут |
|---|--------|---------------|---------|
| 1 | Информация о документе | *Document Information* | **О** |
| 2 | **Обзор** | *Overview* | **О** |
| 2.1 | ⟶ Цели дизайна | *Design Goals* | о |
| 2.2 | ⟶ Ключевые решения | *Key Design Decisions* | **О** |
| 3 | **Архитектура** | *Architecture* | о |
| 3.1 | ⟶ Контекст системы | *System Context* | о |
| 3.2 | ⟶ Высокоуровневая архитектура | *High-Level Architecture* | о |
| 3.3 | ⟶ Технологический стек | *Technology Stack* | о |
| 4 | Где живёт | *Components and Interfaces* | **О** |
| 5 | Модель данных | *Data Models* | **У** |
| 6 | Контракты / API | *API Design* | **У** |
| 7 | Безопасность | *Security Considerations* | о |
| 8 | Обработка ошибок | *Error Handling* | о |
| 9 | Производительность | *Performance Considerations* | о |
| 10 | Тестирование | *Testing Strategy* | о |
| 11 | Развёртывание | *Deployment and Operations* | о |
| 12 | Миграция и совместимость | *Migration and Compatibility* | **У** |
| 13 | Сверка с конституцией | *(— LX: Constitution Check)* | **О** |
| 14 | Инвариант-контроль | *(— LX-native)* | **О** |
| 15 | Чек-лист ревью дизайна | *Design Review Checklist* | о |

Приходит в PLAN: `Где живёт` (из SPEC → §4), `Схема данных` (из SPEC → §5/6/12),
enforcement инвариантов (§14 ссылается на `invariants.md`).
Уходит из PLAN: `Изменяемые/Новые файлы`, `Фазы реализации` → TASKS (D3).

---

## 5. Целевой `invariants.md` (durable-дом инвариантов — D4/D5)

| # | Секция | Оригинал Kiro | Атрибут |
|---|--------|---------------|---------|
| 1 | Информация о документе | *Document Information* | **О** |
| 2 | Тест-матрица (сводка INV × тест × статус маркера) | *(— LX-native)* | о |
| 3 | **Записи инвариантов (INV1…INVn)** | *(— LX-native)* | **О** |
| 3.n | ⟶ Утверждение (EARS-контракт) | *(—)* | **О** |
| 3.n | ⟶ Зонтик конституции §N | *(—)* | **О** |
| 3.n | ⟶ Почему (R\<n\> / инцидент) | *(—)* | **О** |
| 3.n | ⟶ Где в коде (`file:symbol`) | *(—)* | **У** |
| 3.n | ⟶ Тест (`test_name`) | *(—)* | **У** |
| 3.n | ⟶ Маркер `// SPEC NNN` (planned→placed) | *(—)* | **О** |
| 3.n | ⟶ Дыры покрытия | *(—)* | о |

Дом инвариантов независимо от числа — порога нет. Ссылки двунаправленные: SPEC (строка-ссылка) ↔
`invariants.md` ↔ PLAN §14; `/speckit.analyze` сверяет все три.

---

## 6. Карта переноса (что куда уезжает)

```text
SPEC (было)                    →  куда уходит
──────────────────────────────────────────────────
Где живёт (карта кода)         →  PLAN §4  Где живёт (Components)
Схема / контракт данных        →  PLAN §5/6  Модель данных / API
   › Migration / back-compat   →  PLAN §12  Миграция и совместимость
Инварианты (полное тело)       →  invariants.md (дом), строка-ссылка ← остаётся в SPEC

PLAN (было)                    →  куда уходит
──────────────────────────────────────────────────
Изменяемые файлы               →  tasks.md  (задачи по файлам)
Новые файлы                    →  tasks.md  (задачи по файлам)
Фазы реализации                →  tasks.md  Implementation Plan (Phase 1…N)
```

---

## 7. Что добавляется из Kiro (не было у LX)

- **SPEC:** Навигация, `Что входит` (in-scope — LX фиксировал только вне-scope),
  Нефункциональные требования, Ограничения и допущения, Глоссарий, Чек-лист ревью требований.
- **PLAN:** Цели дизайна, вся Архитектура (Контекст/Высокоуровневая/Стек), Обработка ошибок,
  Безопасность, Производительность, Тестирование, Развёртывание, Чек-лист ревью дизайна.

---

## 8. Что сохраняется как LX-специфика (нет у Kiro)

- **SPEC:** Текущее состояние (baseline), Отклонённые альтернативы, Связи (+ Second Brain `[[note]]`).
- **PLAN:** Сверка с конституцией, **Инвариант-контроль** (planned→placed маркеры).
- **`invariants.md`** целиком — durable-технический контракт-слой; у Kiro прямого аналога нет.
- **Трассировка маркеров `// SPEC NNN`** — механизм код→спека, богаче текстовой traceability Kiro.
- **Визуальные конвенции** (mermaid, EARS, правило холодного читателя, закрытый словарь emoji,
  моноширинное выравнивание широких matrix-таблиц — `|` ровными колонками в raw-diff).

---

## 9. Открытые пункты / следующий шаг

- [ ] Сгенерировать `spec-template.md`, `plan-template.md`, `invariants-template.md` по этим скелетам.
- [ ] Перенести sequencing-секции в `tasks-template.md`.
- [ ] Обновить `/speckit.analyze`: сверка трёх точек инварианта (SPEC-ссылка ↔ `invariants.md` ↔ PLAN §14).
- [ ] Решить формат маркера `Жанр` во front-matter и то, как он влияет на обязательность секций
      (для E-жанра `Текущее состояние` фактически обязательна; для P-жанра — опциональна).
- [ ] Прогнать одну реальную `sing-box-lx` спеку через новую раскладку как валидацию.

---

## 10. Дополнение v4.1: Kiro-требования, ренеймы, без нумерации

Пост-тюнинг после обкатки v4 на корпусе (18 спек polymarket-experiments). Решение — D10.

### Целевой SPEC v4.1 (заменяет скелет §3)

```text
# SPEC NNN — Title

## Информация о документе        (Status/Type/Жанр/Depends/Supersedes/Schema/Release)
## Навигация                     ·LX· опц.
## Введение
   ### Краткое описание          ЧТО (= Feature Summary; было «Цель»)
   ### Ценность: проблема → решение   ЗАЧЕМ (= Business Value; было «Проблема»)
   ### Текущее состояние (до работы)  ·LX· as-built baseline (E — фактически обязательна)
   ### Границы                   В scope / Вне scope (= Scope)
## Требования                    per-req блоки (Kiro):
   ### R1 — [заголовок]
       **User Story** (P — обязат., E — опц.)
       #### Критерии приёмки (EARS)   WHEN/IF/WHILE/WHERE SHALL + (test_name)
       #### Детали                    Приоритет · Сложность · Зависимости · Допущения (опц.)
## Инварианты                    ·LX· индекс + зонтик + футганы; сразу за Требованиями
## Нефункциональные требования   опц., в EARS
## Ограничения и допущения       опц.; внешние рамки (≠ инварианты)
## Риски                         ·LX· опц.
## Критерии успеха               (= Success Criteria)
   ### Definition of Done
   ### Метрики приёмки           опц.
## Отклонённые альтернативы      ·LX· опц.
## Связи (трассируемость)        ·LX·
## Глоссарий                     усл.
## Чек-лист ревью требований     опц.
```

### Почему снята нумерация (пересмотр части D7)

Эмпирика корпуса: опциональные секции опускаются по-разному в каждой спеке, а нумерация
«подряд без пропусков» делает один и тот же раздел §6 в одной спеке и §9 в другой —
кросс-ссылка «§N» неоднозначна на уровне корпуса. Точную адресацию несут стабильные
ID-сущности (`R1`, `INV2`, `D1`), на секции GitHub даёт якоря по имени. PLAN и
invariants.md — та же конвенция (без номеров, ссылки по именам секций).

### Размещение Инвариантов (после Требований)

R и INV — пара по оси «что»: требования = что система делает, инварианты = что при этом
никогда не ломается; в корпусе они перекрёстно ссылаются (006: R1 Parity ↔ INV1).
Главный читатель секции — тот, кто идёт менять код: ему нужны оба списка одним проходом,
и WARNING-футганы должны встретиться рано. Kiro-вариант (кластер с Constraints) отвергнут:
Constraints у Kiro — внешние рамки, LX-инвариант — внутреннее обязательство (см. D8/D9);
смешение стирает различие. Итоговая лесенка: обязательства → гарантии → качества → рамки → угрозы.
